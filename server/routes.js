const HOME = "/";

const SENSOR = "/sensor";
const MASK = "/mask";
const MYSQL = "/mysql";

const routes = {
    home : HOME,
    sensor: SENSOR,
    mask: MASK,
    mysql: MYSQL
};


export default routes;